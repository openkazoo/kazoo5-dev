{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "_id": "system_config.attachments.s3",
    "description": "Schema for AWS S3 attachment handler system config",
    "properties": {
        "allow_metadata_credentials": {
            "default": false,
            "description": "Whether to allow using AWS task or instance metadata credentials for authentication. Not recommended if storage plans are openly configurable (e.g. by users) on your system.",
            "type": "boolean"
        }
    },
    "type": "object"
}
