# Getstream

## About Getstream

## Schema



## Fetch

> GET /v2/accounts/{ACCOUNT_ID}/getstream

```shell
curl -v -X GET \
    -H "X-Auth-Token: {AUTH_TOKEN}" \
    http://{SERVER}:8000/v2/accounts/{ACCOUNT_ID}/getstream
```

## Create

> PUT /v2/accounts/{ACCOUNT_ID}/getstream

```shell
curl -v -X PUT \
    -H "X-Auth-Token: {AUTH_TOKEN}" \
    http://{SERVER}:8000/v2/accounts/{ACCOUNT_ID}/getstream
```

## Patch

> PATCH /v2/accounts/{ACCOUNT_ID}/getstream

```shell
curl -v -X PATCH \
    -H "X-Auth-Token: {AUTH_TOKEN}" \
    http://{SERVER}:8000/v2/accounts/{ACCOUNT_ID}/getstream
```

## Remove

> DELETE /v2/accounts/{ACCOUNT_ID}/getstream

```shell
curl -v -X DELETE \
    -H "X-Auth-Token: {AUTH_TOKEN}" \
    http://{SERVER}:8000/v2/accounts/{ACCOUNT_ID}/getstream
```

## Create

> PUT /v2/accounts/{ACCOUNT_ID}/getstream/connect

```shell
curl -v -X PUT \
    -H "X-Auth-Token: {AUTH_TOKEN}" \
    http://{SERVER}:8000/v2/accounts/{ACCOUNT_ID}/getstream/connect
```

