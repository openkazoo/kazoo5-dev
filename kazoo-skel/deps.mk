# To add 3rd party dependencies, uncomment the following line and add
# the name of the dependency

# DEPS =

# If the dep is "foobar" this should be: DEPS foobar
# Then, below here, define how the dependency should be fetched
# Prefix the dependency name with "dep_" and specify the location of
# the dep
# See https://erlang.mk/guide/deps.html#_adding_dependencies_to_your_project

# dep_foobar = git https://github.com/2600hz/erlang-foobar {BRANCH_OR_TAG}
